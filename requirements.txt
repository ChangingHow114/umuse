# U Muse — Python 依赖
# Phase 1 核心依赖

# === ML 框架 (macOS: MPS / Windows: CUDA) ===
torch>=2.5.0
torchaudio>=2.5.0
demucs>=4.0.1
onnxruntime>=1.18.0

# === 音频处理 ===
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0
scipy>=1.11.0
numpy>=1.24.0

# === MIDI & 乐谱 (Phase 2-3) ===
pretty_midi>=0.2.10
mido>=1.3.0
music21>=9.1.0
mir_eval>=0.7       # basic-pitch ONNX 模式依赖 (pip install 时可能遗漏)
jianpu-ly>=1.8      # 简谱 (numbered notation) 生成

# === 机器学习工具 (Phase 4) ===
scikit-learn>=1.3.0

# === 音频效果器 (Phase 5) ===
pedalboard>=0.9.0

# === GUI (Phase 6) ===
PySide6>=6.6.0

# === 工具 ===
PyYAML>=6.0
tqdm>=4.66.0
