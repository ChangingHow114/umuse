# 2026-07-04 — Mac M1 环境适配

## 完成

### 环境从 Windows → Mac M1 适配
- 确认 Python 3.13.7 + venv 全量依赖可用
- ffmpeg / LilyPond / Homebrew 确认已安装在 /opt/homebrew/bin
- PyTorch MPS (Apple GPU) 可用, 替代 CUDA

### 修复的跨平台 Bug (3 个)
1. **LilyPond 路径硬编码** (main.py:294)
   - 原: `set_lilypond_path("F:/UMuse/lilypond/.../lilypond.exe")`
   - 改: 调用 `get_lilypond_path()` 自动检测 (支持 Homebrew 路径)

2. **music21 v10 API 兼容** (midi_to_score.py:221)
   - Chord 对象不再有 `.nameWithOctave` 属性
   - 改为区分 Note/Chord 分别处理

3. **music21 v10 API 兼容** (notation_formats.py:655,760)
   - `n.pitches` 直接返回 Pitch 对象, 不需要再 `.pitch`
   - 改为 `p.midi` / `list(n.pitches)`

### 补充依赖
- `mir_eval` — basic-pitch ONNX 模式遗漏的依赖
- `jianpu-ly` — 简谱生成依赖

### 验证结果
- Phase 1 分轨: ✅ 15s 音频 → 6 stems (Roformer + Demucs)
- Phase 2 MIDI: ✅ Piano 24→22 notes, 其他 4 轨也正常
- Phase 3 乐谱: ✅ 五线谱 PDF / 总谱 PDF / 简谱 (有警告)
- Phase 6 GUI: ✅ 所有模块导入成功
- Pipeline 全链路: ✅ Phase 1→2→3 一键运行成功

## 待办
- [ ] 测试 GUI 可视化运行 (需要图形界面环境)
- [ ] Phase 4 音色预设匹配 (依赖已装好)
- [ ] Phase 5 效果器参数预估 (依赖已装好)
- [ ] Mac 上 MPS device 支持 (目前用 cpu, 代码中可加 mps 选项)
